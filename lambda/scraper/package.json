{
  "name": "ai-slug-navigator-scraper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install && npx prisma generate && zip -r ../../terraform/scraper-lambda.zip . -x '*.ts' 'tsconfig.json' 'node_modules/.cache/*'"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "@prisma/client": "^5.10.2",
    "axios": "^1.13.2",
    "cheerio": "^1.1.2"
  },
  "devDependencies": {
    "prisma": "^5.10.2"
  }
}
