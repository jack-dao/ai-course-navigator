{
  "name": "ai-slug-navigator-scraper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "package": "npm install && npx prisma generate && npm run build && zip -r ../../terraform/scraper-lambda.zip handler.js package.json node_modules prisma -x 'node_modules/.cache/*' '*.ts' 'tsconfig.json'"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "@prisma/client": "^5.10.2",
    "axios": "^1.13.2",
    "cheerio": "^1.1.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.10.0",
    "prisma": "^5.10.2",
    "typescript": "^5.7.0"
  }
}
